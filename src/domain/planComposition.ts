// Rules that compose a pathway into the month stack, plan cards, and
// builder summaries shown across the app.
export {
  blockWhy,
  blockPlain,
  planPlain,
  buildMonthStack,
  buildPlanCards,
  builderSummaries,
} from "@/features/stretch/StretchPrototype";
export type {
  MonthBlock,
  MonthCardSummary,
  StackStatus,
} from "@/features/stretch/StretchPrototype";
