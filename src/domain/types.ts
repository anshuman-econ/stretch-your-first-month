// Shared domain types. Re-exported from the Stretch feature module,
// which currently owns the canonical declarations.
export type {
  Step,
  PathwayKey,
  JourneyTab,
  Pathway,
  PassportStamp,
  StackStatus,
  MonthBlock,
  DemoTile,
  CatalogOption,
  ControlledSwap,
  MonthCardSummary,
  PodDetail,
  KitCategory,
  PackMeta,
} from "@/features/stretch/StretchPrototype";
